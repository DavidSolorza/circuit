# Maintainability Rules

1. Every file has a single clear purpose
2. Imports are organized: external -> internal, absolute -> relative
3. No circular dependencies
4. Tests mirror source structure
5. Config is centralized in core/config/
6. Types are centralized in shared/types/
