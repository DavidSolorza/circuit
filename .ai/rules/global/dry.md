# DRY (Don't Repeat Yourself)

1. Extract repeated logic into shared utilities (shared/lib/)
2. Extract repeated UI patterns into shared components (shared/components/ui/)
3. If you write the same code 3+ times, abstract it
4. Use constants for repeated values (config, types)
5. Prefer composition over copy-paste
