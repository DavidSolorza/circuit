# Database Schema Rules

1. Use Mongoose with TypeScript interfaces
2. Enums validated at schema level
3. Timestamps: true on all models
4. Indexes on foreign keys and frequently queried fields
5. Subdocuments for embedded relationships (stages in paths)
6. References for separate collections (user -> paths)
