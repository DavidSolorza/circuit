# Validation Rules

1. Validate required fields: return 400 with specific error message
2. Validate email format on registration
3. Validate enum values match the schema
4. Validate string lengths to prevent abuse
5. Validate ObjectId format for MongoDB IDs
