# Security Rules

1. Sanitize all user inputs
2. Use CORS with specific origin (not *)
3. Rate limiting on auth endpoints (future)
4. No sensitive data in error messages
5. Helmet middleware for security headers (future)
6. Environment variables for all secrets
