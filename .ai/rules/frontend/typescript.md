# TypeScript Rules

1. Strict mode enabled
2. Prefer interfaces over types for object shapes
3. Use `type` for unions, intersections, and mapped types
4. Avoid `any` — use `unknown` and narrow when needed
5. Use `as const` for literal types
6. Use satisfies operator for complex type validation
7. Use optional chaining and nullish coalescing
