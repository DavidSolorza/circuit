# Component Rules

1. Components are functions, not classes
2. One component per file
3. Props are typed with interface exported
4. Default export for page components, named export for shared components
5. Use composition over props drilling
6. Children prop is typed as ReactNode
7. Event handlers: handle{EventName} naming
