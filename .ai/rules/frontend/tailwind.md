# TailwindCSS v4 Rules

1. Use only Tailwind utility classes in JSX
2. Extract repeated class patterns into compose patterns if needed
3. Use @apply sparingly (prefer utility classes)
4. Use the project's color scale (primary, neutral, etc.)
5. Responsive: mobile-first with sm:, md:, lg:, xl: prefixes
6. Dark mode: use dark: prefix
