# Accessibility Rules

1. All interactive elements must be keyboard accessible
2. Use semantic HTML (button, nav, main, section, etc.)
3. Add aria-label to icon-only buttons
4. Ensure color contrast meets WCAG AA standards
5. Focus indicators must be visible
6. Use proper heading hierarchy (h1 -> h2 -> h3)
