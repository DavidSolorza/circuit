# DevOps — CI/CD

- GitHub Actions for CI: lint -> build -> test
- Deploy frontend to Vercel or Netlify
- Deploy backend to Railway or Render
- Environment variables set in CI secrets
- On push to main: auto-build and deploy
- PR checks: build + tests must pass before merge
