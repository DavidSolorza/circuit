# DevOps — Docker

- Multi-stage Dockerfile for frontend (nginx) and backend (node)
- Docker compose for local development (frontend + backend + mongo)
- .dockerignore to exclude node_modules, dist, .env
- Environment variables via --env-file or docker-compose env_file
